# Development: run Vite dev server so changes hot-reload.
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
# Drop privileges. A process that only serves requests has no need for
# root, and a container escape starts from whatever the process is.
USER node

EXPOSE 3000
CMD ["npm", "run", "dev"]
